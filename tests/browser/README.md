# Browser harness tests

This directory contains browser-level checks of test infrastructure. Tests marked
`@target` approve only the harness capability they name; they do not approve
baseline application defects.

`HARNESS-STORAGE-FAULTS-001` verifies controlled missing-value and failure injection.
