# P2.0 baseline evidence

## Preserved baseline

- Repository: `https://github.com/Frnit/Tulenjohtolaskuri.git`
- Audited branch: `main`
- Audited commit: `797d77ff72d28c5a1e88b82e4155c2d0de93dbd2`
- Commit tree: `9957ba85a6397587b313572a2af5b74eac8778fb`
- Annotated tag: `phase0-baseline-797d77f`
- Integration branch: `next`
- Verification time: `2026-09-11T15:29:25Z`

The annotated tag and the initial `next` branch both resolve to the audited
commit above. The tag annotation is:

> Phase 0 audited baseline before controlled Phase 2 implementation

## Checks performed

```text
git fetch --prune origin
git status --short --branch
git rev-parse HEAD
git rev-parse refs/remotes/origin/main
git branch -a --no-color
git tag --list
git rev-list -n 1 phase0-baseline-797d77f
git rev-parse main
git rev-parse next
```

Observed before the first repository write:

- remote `main` resolved to the expected audited commit;
- the new local worktree was clean;
- the remote had only the `main` branch and no tags;
- no unexpected remote change was present.

After local reference creation, `main`, `next`, and the dereferenced annotated
tag all resolved to `797d77ff72d28c5a1e88b82e4155c2d0de93dbd2`.

## Main preservation

No commit was created on `main`, its ref was not moved, and no production file
was changed during P2.0. The baseline content remains identified by commit and
tree SHA above.

## Remote publication status

Remote refs were verified after publication with `git ls-remote --heads --tags
origin`:

- `main` remained at `797d77ff72d28c5a1e88b82e4155c2d0de93dbd2`;
- `next` pointed to the P2.0 evidence commit, whose parent is the audited baseline;
- tag object `6ed451cf3bd9843a6288d5390e160a0ae370388d` dereferenced to the audited baseline;
- no force push or `main` update was performed.

Publication verification time: `2026-09-11T18:53:23+03:00`.
